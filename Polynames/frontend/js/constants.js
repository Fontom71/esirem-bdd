// URL de l'API pour les requêtes HTTP
export const API_URL = "http://localhost:8080";

// URL pour les événements SSE (Server-Sent Events)
export const SSE_URL = "localhost:8080";

// Taille minimale de l'écran
export const MIN_SCREEN_WIDTH = 800;
